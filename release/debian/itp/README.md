# ITP draft status

Status: **NOT FILED**. Rafael Laboissière advised waiting to file the Debian
Octave ITP until the `octave-team` Salsa repository is created. The repository
creation/access request is pending. These drafts contain no fabricated bug
numbers; they are not yet ready to file. Recheck WNPP and package availability
immediately before filing, then use the current Debian Developer's Reference
procedure.

File in dependency order:

1. `gmpfrxx-mkii.md`
2. `mplapack.md`
3. `octave-mplapack-interop.md` (historical filename/content; update to the
   team-directed Debian source/binary name `octave-mplapack` before filing)

The source archives, release commits, SHA256 values, and current local QA
limitations are recorded in `../PROVENANCE.md`, `../SHA256SUMS`, and
`../QA-EVIDENCE.md`. Do not describe the current source-only packaging
skeletons as Debian QA PASS.
