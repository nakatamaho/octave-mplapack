# Native build support belongs to M01.
# A future build must honor MKOCTFILE and discover mplapack_mpfr via pkg-config.

.PHONY: all clean

all:
	@echo "Native module build is not implemented before M01" >&2
	@exit 1

clean:
	@echo "Nothing to clean during M00"
