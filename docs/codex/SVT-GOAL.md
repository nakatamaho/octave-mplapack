# SVT execution prompt

Use the user's selected Luna model with xhigh reasoning in the Codex session at
the octave-mplapack repository root. Place the complete companion directory, not
only the top-level specification. Model selection is a session setting; the text
below does not change or assert availability of a particular model identifier.

```text
/goal Implement docs/codex/SVT-LUNA-XHIGH.md and all four companion specifications plus docs/codex/svt/cases.json for the mplapack-interop Octave package. Read AGENTS.md and all applicable local instructions first. Execute SVT00 through SVT19 sequentially, run each gate, and write svtXX-report.md before advancing. Continue automatically after PASS; stop dependent work on a real blocker and provide a minimal reproducer. Preserve and reuse working NEIG/earlier SVD examples without assuming that a specification means an implementation exists. Implement all Tier S and Tier A cases, and the mandatory self-contained Tier V baselines for all singular-value inclusions, positive cluster projectors, compatible simple-pair factor boxes, and spectral/inverse-norm certification. Tier V is not satisfied by precision agreement, nonrigorous residuals, bibliography, empty adapters, or an optional external dependency. Audit the local scalar rounding/precision/range contract before using the stated outward-arithmetic proof. Use only existing public mp operations; do not add a backend, SVD driver, installed API, rounding setter, dependency, or MPLAPACK header change. Separate exact models, frozen solver inputs, work precision, reference precision, and certificate targets. Exactly widen returned values for diagnostics; never recompute and substitute a higher-precision answer. Run all 120 smoke SVD rows and 184 demo SVD rows, separately counted mandatory V jobs, adversarial checker tests, existing relevant regressions, and isolated clean-package QA. Stress is opt-in. Use the exact dyadic certificate serialization and replay tests, fixed acceptance targets, and explicit CERTIFIED/INCONCLUSIVE/UNSUPPORTED/error distinctions. Label the V methods as the specified conservative baselines, not full reproductions of the Rump paper algorithms. Do not force native failures, compare repeated vectors individually, treat nonrigorous reference agreement as verification, silently weaken gates or widen work inputs, use a binary64 numerical fallback, overwrite unrelated work or output, or push/merge/tag/publish. Do not stop at a plan. Finish with actual changed files, exact commands, measured S/A/V results, all 20 milestone statuses, source attribution, and honest limitations; unexecuted tests are never PASS.
```
