# Ready-to-send ITP drafts

Status: **NOT FILED**. These drafts are prepared for a maintainer with a
configured BTS/reportbug identity. They contain no fabricated bug numbers.
Recheck WNPP and current Debian package availability immediately before
filing, then submit one `wnpp` report per package using the current Debian
Developer's Reference procedure.

File in dependency order:

1. `gmpfrxx-mkii.md`
2. `mplapack.md`
3. `octave-mplapack-interop.md`

The source archives, release commits, SHA256 values, and current local QA
limitations are recorded in `../PROVENANCE.md`, `../SHA256SUMS`, and
`../QA-EVIDENCE.md`. Do not describe the current source-only packaging
skeletons as Debian QA PASS.
